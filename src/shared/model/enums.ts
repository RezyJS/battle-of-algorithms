export enum DirectionX {
  LEFT = -1,
  STAY = 0,
  RIGHT = 1,
}

export enum DirectionY {
  TOP = -1,
  STAY = 0,
  DOWN = 1,
}

export enum FieldContent {
  EMPTY = 'empty',
  WALL = 'wall',
  KEY = 'key',
  EXIT = 'exit',
}
