- [ ] Добавить realtime-обновления в приватных битвах, чтобы статусы и готовность не обновлялись только после reload.
- [ ] Привести `infra/docker-compose.yml` в репозитории к production-схеме, которая уже работает на VPS.
- [ ] Добавить healthcheck для `backend` и `frontend` в compose.
- [ ] Дожать Docker-слои и build context ещё сильнее, если сборка на VPS останется долгой.
- [ ] Убрать `email` scope из OIDC flow, если окончательно отказываемся от email при регистрации в Keycloak.
- [ ] Сделать так, чтобы назначение участников происходило только по кнопке подтвердить. То есть мы собираем участников, назначаем режимы, выбираем какую карту делаем - случайная/фиксированная/конструктор, а потом уже нажимаем подтвердить и всё отправляется работать (конструктор подстроить под эту логику)
- [ ] Добавить фильтрацию и пагинацию для отправок
- [ ] поменять компоновку карточек настроек в составе арены
